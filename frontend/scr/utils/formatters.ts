// Legacy formatters - Re-exports from new modular structure
// This file exists for backward compatibility
export {
    formatPrice,
    formatDate,
    formatFileSize,
    formatPhoneNumber,
    truncateText,
    formatCurrency,
    CurrencyFormatter,
    DateFormatter,
    FileSizeFormatter,
    PhoneFormatter,
    TextFormatter,
} from './formatters/index';